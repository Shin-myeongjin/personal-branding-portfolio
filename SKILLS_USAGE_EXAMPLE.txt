// App.jsx에 추가하는 방법:

// 1. import 추가
import SkillsFlow from './components/SkillsFlow';

// 2. 원하는 위치에 컴포넌트 추가 (예: About 섹션 다음)
<SkillsFlow />

// 전체 예시:
function App() {
  return (
    <>
      <HeaderFixed />
      <Hero />
      <SplitTextSection id="about" ... />
      <AboutSearch />
      <AboutProfile />
      
      {/* 여기에 추가! */}
      <SkillsFlow />
      
      <Keywords />
      {/* ... 나머지 컴포넌트들 */}
    </>
  );
}
